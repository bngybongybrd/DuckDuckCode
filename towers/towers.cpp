#include <bits/stdc++.h>
#define int long long
using namespace std;

// initialise arr of sets
// go through [k1, k2, k3, ...]
// compare each element which every set (find smallest number in set)
// find the tower with the top (number) that is closest to the element
// and add the element on it. If element cannot be put in any set,
// create another set with that element.
// Output number of sets (towers)


signed main(){
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	
	return 0;
}
